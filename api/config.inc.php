<?php
	# Path for spool directory, Webserver user needs write access
	$spool_path = "/var/www/tmp";
	# Secret for the API connection, must be the same on agent and api
	$secret = "secret";
	# If you want debug output to json.log logged set it to True
	$debug = False;
?>